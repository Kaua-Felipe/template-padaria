import styled from "styled-components"

export const PageMenus = styled.div`
    width: 100%;
`
export const ContainerMenus = styled.main`
    background-color: var(--white);
    color: var(--medium-orange);
    padding: 100px 0;
    
    h1 {
        font-size: 33px;
        font-weight: lighter;
        width: 100%;
        text-align: center;
        color: var(--medium-orange);
    }
    > p {
        font-size: 18px;
        font-weight: lighter;
        text-align: center;
        margin: 25px 0 70px 0;
    }
`
