import { keyframes } from "styled-components"

export const borderBottomAnimation = keyframes`
    from {
        border-bottom: 1.5px solid var(--medium-orange);
    }
    to {
        border-bottom: 1.5px solid var(--white);
    }
`
